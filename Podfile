platform :osx, '11.0'

target 'ClipboardManagerMac' do

  # used for JSON serialization
  pod 'SwiftyJSON', '~> 4.0'

  # used for running a socket server
  pod 'Swifter', '~> 1.5'

  # used for client connections to server
  pod 'Socket.IO-Client-Swift', '~> 16.0'
  
end
